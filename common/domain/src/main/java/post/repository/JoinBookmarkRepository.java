package post.repository;


import post.entity.JoinBookmark;
import org.springframework.data.jpa.repository.JpaRepository;

public interface JoinBookmarkRepository extends JpaRepository<JoinBookmark, Long> {
}
